"""
The parser takes in a string formatted as follows: AMOUNT SOURCE_UNIT in DESTINATION_UNIT and outputs the
relavent components as an array
"""
def parser(string):
    sepstring = string.split(' ') #breaks the string into parts of an array wherever there is a space
    return sepstring
    